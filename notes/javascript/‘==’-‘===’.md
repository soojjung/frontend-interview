# ‘==’과 ‘===’의 차이점

"=="는 **동등 연산자**로, 비교하는 대상의 **타입**이 다르더라도, 타입을 **강제로 변환**시켜 타입을 같게 한 뒤 비교합니다. 즉, 두 값의 타입이 다르더라도, **형 변환된 값**이 같다면 true를 리턴합니다.

"==="는 **일치 연산자**로 **타입**과 **값**이 모두 같을 경우에만 true를 리턴합니다.
